TASK (gopherstack polecat, branch polecat/amber/identity):
Fix "Identity & security" popular-service parity gaps in parity.md.
1. Read parity.md "## Identity & security" (~L892-962): IAM, STS, KMS, Cognito, SecretsManager, etc. Each gap cites file:line.
2. Implement every cited gap in services/<svc>/ with real AWS behavior. No stubs/TODO/placeholders. No dead/unwired code.
3. Table-driven Go tests per fix. Work SERIALLY (no parallel subagents).
4. HARD: no tooling/lockfile/Makefile/CI/go.mod changes. Only services/<svc>/. No .py/.sh/.js. Do NOT commit .sling.md.
5. Before commit fix to green yourself: go build ./..., go test ./services/<touched>/..., golangci-lint run ./services/<touched>/... = 0 issues (NO //nolint; complexity<20; nlreturn; lines<=120; t.Helper(); NO unused; context.WithoutCancel(ctx); dedup; TestMain not env).
6. git add services/ && commit && push origin polecat/amber/identity. Verify git log origin/parity-sweep-2..HEAD shows your commit before claiming done.
7. Print "IDENTITY DONE" + fixed-gaps list. If low on quota, push what's done + name remaining services.
Do not stop until pushed. If output garbles, stop + restart that step.
