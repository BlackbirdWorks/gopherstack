---
service: azurearm
sdk_module: hashicorp/terraform-provider-azurerm@v4.81.0 # latest 4.x as of 2026-09-08; test/terraform/azure pins "~> 4.0", not a go.mod dependency
last_audit_commit: f973f0c90
last_audit_date: 2026-09-12
overall: B
# Per-op or per-op-family status. Values: ok | partial | gap | deferred.
# wire=response/request shape vs SDK; errors=code+HTTP status; state=real mutate/read; persist=in backendSnapshot.
ops:
  MetadataEndpoints: {wire: ok, errors: n/a, state: n/a, persist: n/a, note: "GET /metadata/endpoints?api-version=2022-09-01. Field set (name, resourceManager, resourceManagerEndpoint, authentication.{loginEndpoint,audiences,tenant}, graph, graphAudience, gallery, portal, suffixes.{storage,keyVaultDns,sqlServerHostname,acrLoginServer}, microsoftGraphResourceId) verified against hashicorp/go-azure-sdk's environments.FromEndpoint, which hard-fails without name/resourceManagerEndpoint/microsoftGraphResourceId. microsoftGraphResourceId is a TOP-LEVEL field on the wire, not nested under a resourceIdentifiers object -- an earlier fix attempt got this wrong and silently zero-valued the field (see AZURE.md section 10.10's M8 entry, bug (3)). Covered by TestBuildMetadataEndpoints, which asserts every field's presence explicitly."}
  OpenIDConfiguration: {wire: ok, errors: n/a, state: n/a, persist: n/a, note: "GET /{tenant}/v2.0/.well-known/openid-configuration"}
  InstanceDiscovery: {wire: ok, errors: n/a, state: n/a, persist: n/a, note: "GET /common/discovery/instance?api-version=1.1"}
  Token: {wire: ok, errors: partial, state: n/a, persist: n/a, note: "POST /{tenant}/oauth2/token (v1) and /{tenant}/oauth2/v2.0/token (v2) both return {token_type,expires_in,access_token}. Client-credentials grant only (no auth code/device code flows). Token validation is off by default (--azure-arm-validate-tokens opts in); errors=partial because a malformed grant_type/missing client_id currently still issues a token rather than a 400 -- see gaps."}
  Subscriptions: {wire: ok, errors: ok, state: ok, persist: n/a, note: "GET /subscriptions, GET /subscriptions/{sub} -- single fixed dev subscription, always Enabled. GET /subscriptions/{sub} 404s SubscriptionNotFound for any ID other than Settings.SubscriptionID, rather than echoing back whatever ID the caller asked for as if it existed."}
  Tenants: {wire: ok, errors: n/a, state: n/a, persist: n/a, note: "GET /tenants -- single fixed dev tenant"}
  ProviderRegistration: {wire: ok, errors: ok, state: ok, persist: ok, note: "GET /subscriptions/{sub}/providers[/{ns}], POST .../register. registrationState is per-subscription, defaults NotRegistered, tracked in InMemoryBackend.registeredProviders and snapshotted. Only Microsoft.Storage is a real registered ResourceProvider in M7; other namespaces 404 from GetProvider/RegisterProvider but still work via the generic resource pass-through for PUT/GET/DELETE (see families.generic_resource_plane)."}
  ResourceGroupCRUD: {wire: ok, errors: ok, state: ok, persist: ok, note: "PUT/GET/DELETE/LIST /subscriptions/{sub}/resourcegroups[/{name}], case-insensitive on the resourcegroups/resourceGroups segment (verified by TestParseGenericResourcePath and TestHandler_ResourceGroupCRUD). DELETE is idempotent (404-on-delete degrades to 204, matching ARM's own idempotent-delete semantics)."}
  GenericResourcePlane: {wire: ok, errors: ok, state: ok, persist: ok, note: "PUT/GET/DELETE /subscriptions/{sub}/resourceGroups/{rg}/providers/{ns}/{type}/{name}[/{childType}/{childName}...] via a single generic path walker (resourceid.go), dispatched by registry.go to a dedicated ResourceProvider when one is registered for {ns}, else a generic metadata-only pass-through (id/name/type/location/tags/properties.provisioningState=Succeeded). List forms (both subscription-scoped and resource-group-scoped) implemented. Nested child resource types (2+ type/name pairs) parse correctly; Microsoft.ServiceBus (namespaces/queues/topics/authorizationRules, and namespaces/topics/subscriptions at 3 pairs) has dedicated RP behavior as of M9 -- see ops.ServiceBus*. Other namespaces (e.g. Microsoft.DocumentDB, M10) still fall through to the generic metadata-only store."}
  ListKeys: {wire: ok, errors: ok, state: ok, persist: n/a, note: "POST .../{resourceId}/listKeys. Microsoft.Storage/storageAccounts (M7) and Microsoft.ServiceBus/namespaces/authorizationRules (M9) implement it; any other resource type returns ResourceNotFound (matches real ARM's behavior for resource types with no listKeys action, though the error code differs from ARM's own \"operation not supported\" shape -- see gaps)."}
  StorageAccountCRUD: {wire: ok, errors: ok, state: ok, persist: partial, note: "PUT/GET/DELETE/LIST Microsoft.Storage/storageAccounts (rp_storage.go). Metadata-only per AZURE.md section 10.4 -- no data-plane delegation needed since Blob/Queue/Table already discard the account-name segment. Returns sku/kind/location/tags/properties.primaryEndpoints.{blob,queue,table}. Accounts are keyed by name alone (real Azure storage account names are globally unique), with each account remembering its own creating resource group: Put rejects re-creating an existing name under a different resource group (409 StorageAccountAlreadyExists), and Get/Delete/ListKeys/List via the wrong resource group's path 404 rather than reaching another group's account. StorageProvider.Reset/DeleteResourcesInGroup are wired into Handler.Reset (/_gopherstack/reset) and DELETE .../resourceGroups/{name} respectively, so accounts don't survive a reset or outlive their resource group as orphans. persist=partial: the Storage RP's own in-memory account map is NOT yet wired into Handler.Snapshot/Restore -- only the generic InMemoryBackend (resource groups + provider registrations + non-Storage generic resources) is persisted. An ARM-created storage account does not currently survive a snapshot/restore cycle. See gaps."}
  StorageAccountListKeys: {wire: ok, errors: ok, state: ok, persist: n/a, note: "{\"keys\":[{\"keyName\":\"key1\",\"value\":\"<devstoreaccount1 key>\",\"permissions\":\"Full\"},{\"keyName\":\"key2\",...}]}, verified against learn.microsoft.com/en-us/rest/api/storagerp/storage-accounts/list-keys (StorageAccountListKeysResult/StorageAccountKey/KeyPermission schema) -- note \"Full\" (not \"FULL\") is the real enum value. Both keys use pkgs/azureauth.DefaultAccountKey (Azurite's devstoreaccount1 well-known key)."}
  ServiceBusNamespaceCRUD: {wire: ok, errors: ok, state: ok, persist: gap, note: "PUT/GET/DELETE/LIST Microsoft.ServiceBus/namespaces (rp_servicebus.go). Metadata-only, like StorageAccountCRUD: services/azureservicebus (M5) is one-process-one-namespace with no ARM-shaped state of its own, so namespace Put/Delete never call the ServiceBusEntities data-plane adapter -- confirmed by TestServiceBusProvider_NamespacePutGetDelete's explicit assertion that fake.calls stays empty across both. Returns sku.{name,tier} and properties.{provisioningState,status:\"Active\",serviceBusEndpoint}, verified field-for-field against hashicorp/go-azure-sdk@v0.20260709.1191450's SBNamespaceProperties model (all fields omitempty) and resourceServiceBusNamespaceFlatten's dereferences (services/servicebus/servicebus_namespace_resource.go). Namespaces are keyed by (resourceGroup, name); Get/Delete/List via the wrong resource group's path 404/exclude rather than reaching another group's namespace. persist=gap: not yet wired into Handler.Snapshot/Restore, same known gap as StorageAccountCRUD."}
  ServiceBusQueueTopicSubscriptionCRUD: {wire: ok, errors: ok, state: ok, persist: gap, note: "PUT/GET/DELETE/LIST Microsoft.ServiceBus/namespaces/{ns}/queues, /topics, and /topics/{t}/subscriptions (rp_servicebus.go). Unlike namespace Put, these ARE load-bearing against services/azureservicebus via the narrow ServiceBusEntities interface (interfaces.go) -- confirmed by TestServiceBusProvider_{Queue,Topic,Subscription}PutGetDelete's explicit assertion that CreateX/DeleteX are called. Put validates the parent (namespace, or topic for subscriptions) exists first, 404ing ErrServiceBus{Namespace,Topic}NotFound otherwise -- real ARM requires the parent to exist. lockDuration/defaultMessageTimeToLive are ISO-8601 durations parsed/formatted via the new pkgs/iso8601 package (extracted from services/azureservicebus's own parser, mirroring M6's pkgs/odatatable precedent); maxDeliveryCount is a plain integer. Response shapes verified field-for-field against hashicorp/go-azure-sdk's SBQueueProperties/SBTopicProperties/SBSubscriptionProperties models and each resource's own Read function (all consumed fields are omitempty pointers with a corresponding nil-safe dereference, except queue Read's EnablePartitioning check -- gated behind maxSizeInMegabytes being present, which this RP never sets, so it's unreachable here). CreateOrUpdate on all three SDK clients accepts ONLY HTTP 200 (unlike namespaces, which also accepts 201/202) -- verified this RP's PUT path always returns 200 via handleGenericResource's dedicated-provider dispatch (handler_ops.go), not 201-on-create, avoiding the exact class of bug AZURE.md section 10.8's Storage bug (6) found. persist=gap: same known gap as ServiceBusNamespaceCRUD."}
  ServiceBusAuthorizationRuleListKeys: {wire: ok, errors: ok, state: ok, persist: n/a, note: "PUT (metadata-only, any rule name/rights accepted) + POST .../authorizationRules/{name}/listKeys on Microsoft.ServiceBus/namespaces/{ns}. Response shape {primaryConnectionString,secondaryConnectionString,primaryKey,secondaryKey,keyName} verified against hashicorp/go-azure-sdk's namespacesauthorizationrule.AccessKeys model. Both keys/connection-strings are the same fixed dev SAS identity (sbDefaultKeyName/sbDefaultKeyValue, mirroring services/azureservicebus/sas.go's DefaultKeyName/DefaultKeyValue), matching StorageAccountListKeys's identical key1/key2 convention. Critically, ListKeys does NOT require the caller to have PUT an authorizationRules resource first -- only that the parent namespace exists -- since resourceServiceBusNamespaceFlatten (services/servicebus/servicebus_namespace_resource.go) unconditionally calls ListKeys for the fixed \"RootManageSharedAccessKey\" rule on every namespace Read, with no corresponding azurerm_servicebus_namespace_authorization_rule resource in a typical Terraform config."}
  ServiceBusNamespaceNetworkRuleSet: {wire: ok, errors: ok, state: ok, persist: gap, note: "GET/PUT Microsoft.ServiceBus/namespaces/{ns}/networkRuleSets/default (rp_servicebus.go's sbKindNetworkRuleSet, sibling of queues/topics/authorizationRules in classifyServiceBusPath's 2-pair shape -- no new handler_ops.go route needed since the existing generic path walker already parses this shape correctly). Fixed the real-CI failure found in PR #2466's terraform-tests (6) run (404 ResourceNotFound retrieving the network rule set) -- root-caused by reading terraform-provider-azurerm@v4.81.0's pinned source directly: resourceServiceBusNamespaceFlatten (internal/services/servicebus/servicebus_namespace_resource.go:575) calls client.GetNetworkRuleSet on EVERY namespace Read, unconditionally -- not gated on SKU (this milestone's fixture uses Standard) or on whether the caller's config sets a `network_rule_set` block -- and classifyServiceBusPath had no case for the `networkRuleSets` type segment, so the request fell through to the generic ARM dispatcher and 404'd since nothing had ever PUT a generic resource at that path. hashicorp/go-azure-sdk@resource-manager/servicebus/2024-01-01/namespaces's GetNetworkRuleSet (method_getnetworkruleset.go) confirms the exact path shape (`fmt.Sprintf(\"%s/networkRuleSets/default\", id.ID())`, GET only, always \"default\") and response struct (NetworkRuleSet{properties: NetworkRuleSetProperties{defaultAction, ipRules, publicNetworkAccess, trustedServiceAccessEnabled, virtualNetworkRules}}, all fields pointer/omitempty). Fixed the same way as Storage's GetServiceProperties/{Blob,File}ServiceProperties bugs (AZURE.md section 10.10 M7/M8, bugs (7)/(8)): GET returns 200 with a minimal, schema-valid body (defaultAction:\"Allow\", publicNetworkAccess:\"Enabled\", trustedServiceAccessEnabled:false, empty ipRules/virtualNetworkRules) when no Put has ever been made for that namespace -- the common case, matching what real Azure reports for a namespace with no custom network rules configured -- and echoes back whatever properties a caller does PUT (exercised when terraform-provider-azurerm's createNetworkRuleSetForNamespace runs because a config sets `network_rule_set` explicitly). Both GET and PUT require the parent namespace to already exist (ErrServiceBusNamespaceNotFound otherwise), matching every other ServiceBus child resource's requireNamespace check. Confirmed via TestServiceBusProvider_NetworkRuleSet_{GetWithoutPut,PutThenGet,ParentNamespaceMissing} (rp_servicebus_test.go) -- a live tofu apply confirmation was not possible on this milestone's macOS sandbox because tofu apply fails earlier, at ARM's own metadata-discovery call, due to the pre-existing host-OS TLS-trust gap (AZURE.md section 10.10); expected to be confirmed by the next green CI run on Linux. persist=gap: same known gap as ServiceBusNamespaceCRUD."}
families:
  generic_resource_plane: {status: ok, note: "one path walker (resourceid.go's ParseGenericResourcePath/ParseGenericResourceListPath) handles every namespace/type/nested-child-type shape; registry.go falls back to a metadata-only generic store for any namespace without a dedicated ResourceProvider, so future RPs (M9 ServiceBus -- done, see ops.ServiceBus*; M10 DocumentDB; M13/M14 KeyVault/AppConfiguration) are additive. Namespace dispatch is case-insensitive (registry.go keys/looks up by lowercased namespace), matching ARM's own case-insensitive resource-provider-namespace semantics."}
  endpoint_advertisement: {status: ok, note: "primaryEndpoints.{blob,queue,table} default to http://<account>.{blob,queue,table}.<ARM request's own Host hostname>:10010/ (virtual-hosted-style, one shared port across all three services), captured once at account-creation time from the request context (WithRequestHost/RequestHostFromContext) and reused on subsequent GET/List so the advertised host doesn't change based on which host header a later read happened to arrive on. --azure-arm-advertise-storage-vhost / AZURE_ARM_ADVERTISE_STORAGE_VHOST overrides the host:port per AZURE.md section 10.8 finding (9); superseded the earlier per-service {blob,queue,table}-endpoint overrides once path-style addressing was found incompatible with terraform-provider-azurerm's data-plane SDK. metadata.go's Suffixes.Storage tracks the same host:port dynamically so jackofallops/giovanni's ParseAccountID can strip it back off."}
  auth: {status: ok, note: "Token validation off by default (any client_id/client_secret issues a token; ARM itself accepts any bearer token or none). --azure-arm-validate-tokens, when set, enforces a cryptographically valid Authorization: Bearer token (pkgs/aadauth.Issuer.Parse) before every /subscriptions and /tenants route, returning 401 InvalidAuthenticationToken + WWW-Authenticate on a missing/invalid token; the metadata/discovery/token endpoints stay public regardless (a client can't obtain a token without reaching them). Covered by TestHandler_ValidateTokens_RejectsMissingOrInvalidBearer."}
  tls: {status: ok, note: "The entire services/azurearm listener is served over HTTPS unconditionally (pkgs/devtls self-signed cert, covering localhost/127.0.0.1/::1), per AZURE.md section 10.8's finding that terraform-provider-azurerm hardcodes https:// for metadata_host and validates against the system trust store with no opt-out."}
gaps:
  - "Storage RP account state (StorageProvider.accounts) does not survive a snapshot/restore cycle -- only InMemoryBackend's resource-group/generic-resource/provider-registration state is persisted (see ops.StorageAccountCRUD). Fix is additive: give StorageProvider its own Snapshot/Restore and wire it into Handler.Snapshot/Restore alongside the backend's."
  - "ServiceBus RP state (ServiceBusProvider's namespaces/queues/topics/subscriptions maps) does not survive a snapshot/restore cycle either, the same known gap as StorageAccountCRUD above and for the same reason -- no ResourceProvider's own state is currently wired into Handler.Snapshot/Restore."
  - "TestTerraform_Azure_ServiceBus (M9) could not be confirmed against a live tofu apply on this milestone's local macOS development sandbox, due to the same pre-existing host-OS-specific TLS-trust gap documented for M7/M8 (AZURE.md section 10.10) -- apply fails at ARM's own metadata-discovery call, before reaching any ServiceBus-specific code. Compensating verification: test/integration/azurearm_test.go's TestIntegration_AzureARM_ServiceBusLifecycleAndCrossServiceWiring exercises the identical ARM CRUD + cross-service-wiring path over plain HTTP and passed; wire-shape questions were answered by reading the pinned terraform-provider-azurerm/go-azure-sdk source directly. Should be re-confirmed against a green CI run (expected to work on Linux)."
  - "Token endpoint accepts any (or a missing) client_id/grant_type without validation and always issues a token; a malformed client_credentials request should arguably 400 with invalid_request/unsupported_grant_type per RFC 6749, matching AAD's own error shape more closely."
  - "Direct-data-plane storage resources (azurerm_storage_container/_blob/_queue/_table) are covered as of M8 via the new services/azurestoragevhost virtual-hosted-style listener (AZURE.md section 10.8 finding (9)); azurerm_storage_share (File Share) remains out of scope -- no gopherstack file-share service exists."
  - "Multi-account namespace isolation: all ARM-created storage accounts alias one shared Blob/Queue/Table container/queue/table namespace (services/azureblob/azurequeue/azuretable discard the account-name path segment entirely). Two ARM storage accounts see each other's containers. Deferred to M10 per AZURE.md section 10.4's original note; StorageAccounts.RegisterAccount/DeleteAccount (interfaces.go) is the forward-compat seam for that fix."
  - "ARM error codes are a small hand-picked table (errors.go's errorDetails), not audited against ARM's full published error-code catalog the way services/sqs's errorDetails table has been over multiple audit passes."
  - "management locks, Azure Policy, RBAC/Microsoft.Authorization, ARM template deployments (Microsoft.Resources/deployments), PATCH-merge semantics beyond full-replace, $filter/$expand and nextLink pagination on list operations, cross-subscription/cross-tenant, resource moves, What-If, and LRO polling are all explicitly out of MVP scope per AZURE.md section 10.1/10.3 -- not bugs, deliberate scope boundaries for this milestone."
deferred:
  - "M7 initial implementation. No prior audit passes to report."
  - "M9 added the Microsoft.ServiceBus RP (ServiceBusNamespaceCRUD/ServiceBusQueueTopicSubscriptionCRUD/ServiceBusAuthorizationRuleListKeys). QC pass over the pre-existing implementation found and fixed two real bugs before any Terraform run (List not filtering by resource group for queues/topics/subscriptions; DeleteResourcesInGroup's cascade delete never matching due to a double-pipe key-prefix bug) -- see AZURE.md section 10.10's M9 entry for full detail."
  - "M9's first real-CI run (PR #2466, terraform-tests (6) job on Linux, past the macOS TLS-trust gap) surfaced a third real bug: classifyServiceBusPath had no case for namespaces/{ns}/networkRuleSets/default, which terraform-provider-azurerm's namespace Read calls unconditionally on every namespace regardless of SKU or config -- see ops.ServiceBusNamespaceNetworkRuleSet above and AZURE.md section 10.10's M9 entry for full detail. Checked queue/topic/subscription/authorizationRule Read functions in the same pinned source for the same class of unconditional sub-resource probe; none exists (each Read function only calls its own client.Get)."
leaks: {status: clean, note: "Handler.StartWorker's listener goroutine is the only background goroutine this service starts; covered by leak_test.go's TestMain (pkgs/testleak) plus TestHandler_StartWorker_BindsServesHTTPS/TestHandler_StartWorker_BindFailureIsSynchronous exercising bind/serve/shutdown."}
---

## Notes

### Scope (M7)

This is the first of four ARM milestones (AZURE.md section 10.10): ARM core
(discovery/auth/resource-group CRUD/generic resource plane/provider
registration) plus the `Microsoft.Storage` resource provider only. See
`AZURE.md` section 10 for the full design rationale, and sections 10.1/10.3
for the explicit MVP scope boundaries (listed above under `gaps` as
deliberate, not oversights).

### TLS and the metadata document

`terraform-provider-azurerm` calls `environments.FromEndpoint(ctx,
"https://"+metadataHost)` unconditionally -- there is no configuration that
produces a plain-HTTP metadata request, and the client validates the
certificate against the system trust store with no `InsecureSkipVerify`
equivalent. `services/azurearm`'s entire listener is therefore served over
HTTPS from the first commit, using `pkgs/devtls`'s self-signed certificate.
See `AZURE.md` section 10.8 for the full investigation (verified against
`hashicorp/terraform-provider-azurerm` and `hashicorp/go-azure-sdk` source).

### Terraform test harness status

`test/terraform/azure/` exists as a separate package (own `TestMain`, own
container, fixed published host ports) proving `azurerm_resource_group` and
`azurerm_storage_account` apply/destroy against a real, unmodified
`hashicorp/azurerm` provider. See that package's own doc comment and this
repository's top-level test run output for current pass/skip status --
whether it runs to completion depends on whether the sandbox running it can
both download the `tofu` binary and the `hashicorp/azurerm` provider AND get
the child `tofu`/`terraform` process to trust the self-signed metadata-host
certificate (via `SSL_CERT_FILE`); if either is blocked in a given
environment, the test skips cleanly with a message identifying which
precondition failed, rather than failing the build. The Go unit tests
(`services/azurearm/*_test.go`) and `test/integration/azurearm_test.go`
provide full behavioral coverage independent of the Terraform layer.

### Why `Microsoft.Storage` is metadata-only

`services/azureblob`, `services/azurequeue`, and `services/azuretable` all
parse an account-name path segment and then discard it (`_, container, blob
:= splitPath(...)`), so an ARM-created storage account named `tfstorage123`
is immediately reachable at `http://host:10000/tfstorage123/...` with zero
data-plane changes -- there is nothing for the Storage RP to delegate. The
cost of that shortcut is the shared-namespace gap listed above.

### M8: virtual-hosted-style endpoint advertisement

`terraform-provider-azurerm`'s Blob/Queue/Table data-plane clients (via
`jackofallops/giovanni`) hard-require virtual-hosted-style URLs
(`{account}.{blob,queue,table}.{suffix}`) and reject the path-style URLs
this RP originally advertised (`AZURE.md` section 10.8 finding (9)). Fixed
by advertising `http://{account}.{blob,queue,table}.{host}:10010/`
(`rp_storage.go`'s `advertiseVHostEndpoint`, one shared port across all
three services since the SDK's domain-suffix model has no notion of a port
varying per service) and making `metadata.go`'s `Suffixes.Storage` track
that same host:port dynamically instead of a hardcoded `"localhost"`
literal. The new `services/azurestoragevhost` package is what actually
answers requests on port 10010 -- see that package's own doc comment and
`AZURE.md` section 10.8 finding (9) for the full design.
